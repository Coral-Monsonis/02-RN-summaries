import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20,
    },
    title: {
        fontSize: 22,
        fontWeight: "bold",
        marginBottom: 10,
    },
    subtitle: {
        fontSize: 16,
        marginBottom: 20,
    },
    button: {
        padding: 15,
        backgroundColor: "#eee",
        borderRadius: 8,
        marginBottom: 10,
    },
    buttonText: {
        fontSize: 16,
        textAlign: "center",
    },
})
